# Core Framework
fastapi
uvicorn[standard]
python-multipart

# LangChain & AI
# Pinned to the 0.3.x line — ragas 0.3.7 is incompatible with LangChain v1.x
# (langchain-community 0.4+ removed chat_models.vertexai, which ragas imports).
langchain==0.3.30
langchain-core==0.3.86
langchain-openai==0.3.35
langchain-qdrant==0.2.1
langchain-community==0.3.31
langchain-text-splitters==0.3.11

# Vector Database
qdrant-client

# Document Processing
pypdf
python-docx

# Configuration & Validation
pydantic
pydantic-settings
python-dotenv

# Logging & Monitoring
structlog
langsmith==0.4.55

# HTTP Client
httpx
datasets
ragas==0.3.7